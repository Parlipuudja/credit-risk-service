services:
  ml-service:
    build: .
    ports:
      - "8000:8000"
  gateway:
    build: ./demo
    ports:
      - "8080:8080"
    environment:
      - ML_SERVICE_URL=http://ml-service:8000
    depends_on:
      - ml-service
